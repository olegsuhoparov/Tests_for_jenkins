pipeline {
    agent any
    virtualenv venv --distribute
    cd . venv/Scripts/activate 
    pip install -r requirements.txt
    python tests.py
    stages {
        stage('build') {
            steps {
               bat 'pytest'
            }
        }
    }
}
