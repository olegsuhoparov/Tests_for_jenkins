
pipeline {
    agent any
    environment {
      PROJECT_NAME = "Neptun"
      OWNER_NAME   = "Denis Astahov"
    }

    stages {
    
        stage('test') {
            agent {
                docker {
                    image 'qnib/pytest'
                }
            }
            steps {
              bat 'virtualenv venv && . venv/bin/activate && pip install -r requirements.txt && python --v'
            }
        }
    }
}
