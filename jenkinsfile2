pipeline {
    agent { docker { image 'python:3.8.2' } }
    stages {
        stage('build') {
            steps {
               virtualenv venv --distribute
               . venv/bin/activate 
               pip install -r requirements.txt
               pytest
            }
        }
    }
}
